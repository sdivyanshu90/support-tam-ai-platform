# Runtime
httpx>=0.27               # the only transport needed: both providers are
                          # OpenAI-compatible HTTP, so no vendor SDK is required
pydantic>=2.7,<3          # schemas for every boundary
fastapi>=0.110            # REST interface
uvicorn[standard]>=0.29   # ASGI server
streamlit>=1.36           # thin UI (st.write_stream needs >=1.31)
python-dotenv>=1.0        # .env loading for local development

# Test
pytest>=8.0               # (httpx above also backs fastapi.testclient)

# Deliberately absent: no vector database, no agent framework, no task queue.
# Retrieval is an in-process BM25 index over ~90 chunks (app/retrieval/bm25.py);
# adding a vector store would be more infrastructure than the corpus justifies.
